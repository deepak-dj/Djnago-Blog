name: Django CI

on:
  push:
    branches: [ "*" ]
  pull_request:
    branches: [ "*" ]

jobs:
  build:

    runs-on: ubuntu-latest
    strategy:
      max-parallel: 4
      matrix:
        python-version: [3.9]

    steps:
    - uses: actions/checkout@v4
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v3
      with:
        python-version: ${{ matrix.python-version }}

    - name: Get Commit Message
      id: get_commit
      run: echo "COMMIT_MESSAGE=$(git log -1 --pretty=%B | tr -d '\n' | tr -d '\r')" >> $GITHUB_ENV

    - name: Set Build Name
      run: echo "Build #${{ github.run_number }} - ${{ env.COMMIT_MESSAGE }}"
      
    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        
    - name: Run Docker Compose
      run: docker-compose up -d

    - name: Run Tests
      run: docker-compose run backend python manage.py test

    - name: Tear Down Docker Compose
      run: docker-compose down

    - name: Post Test Cleanup
      if: always()
      run: docker-compose down -v
      
    - name: Set Job Summary
      run: |
        echo "## Build Summary" >> $GITHUB_STEP_SUMMARY
        echo "**Build Name:** Build #${{ github.run_number }} - ${{ env.COMMIT_MESSAGE }}" >> $GITHUB_STEP_SUMMARY

